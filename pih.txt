Set -1

1)Write a Pig Latin scripts to demonstrate distinct, filter, foreach operator on your data.

🔹 Step 1: Sample Data (student.txt)
1,John,80
2,Alice,90
3,Bob,80
4,John,70

🔹 Step 2: Load Data
A = LOAD 'student.txt' USING PigStorage(',') 
    AS (id:int, name:chararrayAA , marks:int);

🔹 Step 3: DISTINCT (remove duplicates)
B = DISTINCT A;

🔹 Step 4: FILTER (condition)
C = FILTER A BY marks > 75;

🔹 Step 5: FOREACH 
D = FOREACH A GENERATE name, marks;

🔹 Step 6: Display Output
DUMP B;
DUMP C;
DUMP D;

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2)Write a Map Reduce Program to perform Word Count Operation on given text file using Hadoop?
🟨 Mapper Class

import java.io.IOException;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.LongWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Mapper;

public class Mapp extends Mapper<LongWritable, Text, Text, IntWritable> {
    public void map(LongWritable key, Text value, Context context)
        throws IOException, InterruptedException {

    String[] words = value.toString().split(" ");
    for (String word : words) {
        context.write(new Text(word), new IntWritable(1));
    }
  }
}

🟨 Reducer Class

import java.io.IOException;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Reducer;

public class WordCountReducer extends Reducer<Text, IntWritable, Text, IntWritable> {

    public void reduce(Text key, Iterable<IntWritable> values, Context context)
            throws IOException, InterruptedException {

        int sum = 0;
        for (IntWritable val : values) {
            sum += val.get();
        }
        context.write(key, new IntWritable(sum));
    }
}

🟨 Driver Class

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Job;
import org.apache.hadoop.mapreduce.lib.input.FileInputFormat;
import org.apache.hadoop.mapreduce.lib.output.FileOutputFormat;

public class WordCountDriver {

    public static void main(String[] args) throws Exception {

        Configuration conf = new Configuration();
        Job job = Job.getInstance(conf, "Word Count");

        job.setJarByClass(WordCountDriver.class);
        job.setMapperClass(Mapp.class);
        job.setReducerClass(WordCountReducer.class);

        job.setOutputKeyClass(Text.class);
        job.setOutputValueClass(IntWritable.class);

        FileInputFormat.addInputPath(job, new Path(args[0]));
        FileOutputFormat.setOutputPath(job, new Path(args[1]));

        System.exit(job.waitForCompletion(true) ? 0 : 1);
    }
}

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3)Write the procedure to create and alter tables with example in Hive Environment.
🟨 Create Table
🔹Step 1: Start Hive
hive

🔹Step 2: Create Database (Optional)
CREATE DATABASE company;
USE company;

🔹Step 3: Create Table
CREATE TABLE emp (
    id INT,
    name STRING,
    salary FLOAT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

🔹 Step 4: Load Data into Table
LOAD DATA LOCAL INPATH '/emp.txt'
INTO TABLE emp;

🔹 Step 5: Display Data
SELECT * FROM emp;

🟨 Alter Table Operations
✔ 1. Add Column
ALTER TABLE emp ADD COLUMNS (age INT);

✔ 2. Change Column Name/Type
ALTER TABLE emp CHANGE salary salary DOUBLE;

✔ 5. Describe Table
DESCRIBE emp;

✔ 3. Rename Table
ALTER TABLE emp RENAME TO employee;

✔ 4. Drop Table
DROP TABLE employee;





Set - 2

1.Implement Stack Data Structure in Java

import java.util.Stack;
public class Stack1 {
    public static void main(String[] args) {
        
        Stack<Integer> s = new Stack<>();
        s.push(1);
        s.push(2);
        s.push(3);
        System.out.println("Stack: " + s);

        System.out.println("Popped: " + s.pop());

        System.out.println("Top element: " + s.peek());

        if (s.search(2) != -1)
            System.out.println("2 is found");
        else
            System.out.println("2 not found");
    }
}

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2.Describe HDFS Commands for Adding Files and Directories

🔹 Create Directory in HDFS
hdfs dfs -mkdir /input

🔹 Upload File (Local → HDFS)
hdfs dfs -put file.txt /input

🔹 List Files
hdfs dfs -ls /input

🔹 View File Content
hdfs dfs -cat /input/file.txt

🔹 Download File (HDFS → Local)
hdfs dfs -get /input/file.txt

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3.Implement the Pig Latin Script to find Word Count

lines = LOAD '/user/hadoop/HDFS_File.txt' AS (line:chararray);
words = FOREACH lines GENERATE FLATTEN(TOKENIZE(line)) AS word;
grouped = GROUP words BY word;
wordcount = FOREACH grouped GENERATE group, COUNT(words);
DUMP wordcount;

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

4.Write the procedure to create, rename, drop tables with example in Hive Environment

hive

CREATE DATABASE company;
USE company;

CREATE TABLE emp (
    id INT,
    name STRING,
    salary FLOAT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

ALTER TABLE emp RENAME TO employee;

DROP TABLE employee;




Set- 3

1.Implement Linked Lists Data Structure in Java

import java.util.LinkedList;

public class Main1 {
    public static void main(String[] args) {

        LinkedList<String> list = new LinkedList<>();

        list.add("Java");
        list.add("Python");
        list.add("C++");

        System.out.println("List: " + list);

        list.remove(1);

        System.out.println("After remove: " + list);
    }
}

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2.Implement the Pig Latin Script to find the max temp for each and every year

records = LOAD '/home/hadoop/sample.txt'
USING PigStorage(',')
AS (year:chararray, temperature:int, quality:int);

filtered_records = FILTER records BY temperature != 9999 AND
(quality == 0 OR quality == 1 OR quality == 4 OR quality == 5 OR quality == 9);

grouped_records = GROUP filtered_records BY year;

max_temp = FOREACH grouped_records GENERATE group,
MAX(filtered_records.temperature);

DUMP max_temp;

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3.Write the procedure to load the data using internal tables in Hive Environment.
🔹 Step 1: Start Hive
hive

🔹 Step 2: Create Database (Optional)
CREATE DATABASE college;
USE college;

🔹 Step 3: Create Internal Table
CREATE TABLE student (
    id INT,
    name STRING,
    marks INT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

🎯 Example Input File (student.txt)
1,John,80
2,Alice,90
3,Bob,70


🔹 Step 4: Load Data into Table
LOAD DATA LOCAL INPATH '/student.txt'
INTO TABLE student;

🔹 Step 5: Verify Data
SELECT * FROM student;

🔹 Step 6: Check Table Structure
DESCRIBE student;

🔹 Step 7: Drop Table (Important)
DROP TABLE student;

🎯 Example Output
1   John   80
2   Alice  90
3   Bob    70

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

4.Describe HDFS Commands for Adding and Deleting Files 
# Create directory
hdfs dfs -mkdir /data

# Upload file
hdfs dfs -put sample.txt /data

# Check file
hdfs dfs -ls /data

# Delete file
hdfs dfs -rm /data/sample.txt


Set - 4

1.Write a Map Reduce Program to perform Word Count Operation on given text file using Hadoop?
🟨 Mapper Class

import java.io.IOException;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.LongWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Mapper;

public class Mapp extends Mapper<LongWritable, Text, Text, IntWritable> {
    public void map(LongWritable key, Text value, Context context)
        throws IOException, InterruptedException {

    String[] words = value.toString().split(" ");

    for (String word : words) {
        context.write(new Text(word), new IntWritable(1));
    }
  }
}

🟨 Reducer Class

import java.io.IOException;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Reducer;

public class WordCountReducer extends Reducer<Text, IntWritable, Text, IntWritable> {

    public void reduce(Text key, Iterable<IntWritable> values, Context context)
            throws IOException, InterruptedException {

        int sum = 0;
        for (IntWritable val : values) {
            sum += val.get();
        }
        context.write(key, new IntWritable(sum));
    }
}

🟨 Driver Class

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Job;
import org.apache.hadoop.mapreduce.lib.input.FileInputFormat;
import org.apache.hadoop.mapreduce.lib.output.FileOutputFormat;

public class WordCountDriver {

    public static void main(String[] args) throws Exception {

        Configuration conf = new Configuration();
        Job job = Job.getInstance(conf, "Word                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           Count");

        job.setJarByClass(WordCountDriver.class);
        job.setMapperClass(Mapp.class);
        job.setReducerClass(WordCountReducer.class);

        job.setOutputKeyClass(Text.class);
        job.setOutputValueClass(IntWritable.class);

        FileInputFormat.addInputPath(job, new Path(args[0]));
        FileOutputFormat.setOutputPath(job, new Path(args[1]));

        System.exit(job.waitForCompletion(true) ? 0 : 1);
    }
}

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2.Write a Pig Latin scripts to demonstrate order by and limit operator on your data.

🔹 Sample Data (student.txt)
1,John,80
2,Alice,90
3,Bob,70
4,David,85

🔹 Step 1: Load Data
A = LOAD 'student.txt' USING PigStorage(',') 
    AS (id:int, name:chararray, marks:int);

🔹ORDER BY Operator
B = ORDER A BY marks DESC;
DUMP B;

✔ Output (ORDER BY)
2,Alice,90
4,David,85
1,John,80
3,Bob,70

🔹LIMIT Operator
C = LIMIT B 2;
DUMP C;

✔ Output (LIMIT)
2,Alice,90
4,David,85

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3.Describe HDFS Commands for Adding and Retrieving Files
🔹 📥 Adding Files to HDFS
✔ Create Directory in HDFS
hdfs dfs -mkdir /input

✔ Upload File (Local → HDFS)
hdfs dfs -put file.txt /input

✔ Verify Upload
hdfs dfs -ls /input

🔹 📤 Retrieving Files from HDFS
✔ View File Content
hdfs dfs -cat /input/file.txt

✔ Download File (HDFS → Local)
hdfs dfs -get /input/file.txt



Set-5

1.Implement Queue Data Structure in Java

import java.util.Queue;
import java.util.LinkedList;

public class QueueExample {
    public static void main(String[] args) {

        Queue<Integer> q = new LinkedList<>();

        q.add(1);
        q.add(2);
        q.add(3);

        System.out.println("Queue: " + q);

        System.out.println("Removed: " + q.remove());

        System.out.println("Front: " + q.peek());

        System.out.println("Size: " + q.size());
    }
}


🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2.Create a table employee with following fields Emp_Id, Ename, Designation, Salary in Hive Environment and display designation and total salary for each job.

🔹Step 1: hive

🔹 Step 2: Create Table
CREATE TABLE employee (
    emp_id INT,
    ename STRING,
    designation STRING,
    salary FLOAT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

🔹 Step 3: Load Data
LOAD DATA LOCAL INPATH '/home/hadoop/emp.txt'
INTO TABLE employee;

🔹 Sample Input File (emp.txt)
101,John,Manager,50000
102,David,Clerk,20000
103,Ram,Manager,60000
104,Ravi,Clerk,25000

🔹 Step 4: Query (Group by Designation)
SELECT designation, SUM(salary) AS total_salary FROM employee
GROUP BY designation;


🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3.Write a Pig Latin scripts to demonstrate different grouping operators on your data.
🔹 Load Data
A = LOAD 'emp.txt' USING PigStorage(',') 
    AS (id:int, name:chararray, dept:chararray, salary:int);

🔹 1. GROUP Operator
B = GROUP A BY dept;
DUMP B;

🔹 2. GROUP ALL (Global Grouping)
C = GROUP A ALL;
DUMP C;

🔹 3. FOREACH with GROUP
D = FOREACH B GENERATE group AS dept, SUM(A.salary) AS total_salary;
DUMP D;

🔹 4. COGROUP (Grouping Multiple Relations)
E = LOAD 'emp.txt' USING PigStorage(',') 
    AS (id:int, name:chararray, dept:chararray, salary:int);

F = COGROUP A BY dept, E BY dept;
DUMP F;

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

4.Describe HDFS Commands for Adding Files and Directories 
🔹 Create Directory
hdfs dfs -mkdir /input

🔹 Upload File
hdfs dfs -put file.txt /input

🔹 Upload Multiple Files
hdfs dfs -put file1.txt file2.txt /input

🔹 Upload Directory
hdfs dfs -put myfolder /input

🔹 List Files
hdfs dfs -ls /input

🔹 View File
hdfs dfs -cat /input/file.txt

🔹 Download File
hdfs dfs -get /input/file.txt



Set - 6

1)Write a Pig Latin scripts to demonstrate distinct, Filter, Foreach operator on your data.

🔹 Step 1: Sample Data (student.txt)
1,John,80
2,Alice,90
3,Bob,80
4,John,70

🔹 Step 2: Load Data
A = LOAD 'st8udent.txt' USING PigStorage(',') 
    AS (id:int, name:chararray, marks:int);

🔹 Step 3: DISTINCT (remove duplicates)
B = DISTINCT A;

🔹 Step 4: FILTER (condition)
C = FILTER A BY marks > 75;

🔹 Step 5: FOREACH 
D = FOREACH A GENERATE name, marks;

🔹 Step 6: Display Output
DUMP B;
DUMP C;
DUMP D;

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2)Write a Map Reduce Program to perform Word Count Operation on given text file using Combiner and Partitioner?
🟨 Partitioner Class

import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Partitioner;

public class Mypat extends Partitioner<Text, IntWritable> {

    public int getPartition(Text key, IntWritable value, int numReduceTasks) {

        if (numReduceTasks == 4) {
            String s = key.toString();

            if (s.length() == 1)
                return 0;
            else if (s.length() == 2)
                return 1;
            else if (s.length() == 3)
                return 2;
            else
                return 3;
        } 
        else {
            return 0;
        }
    }
}

🟨 Driver Class

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Job;
import org.apache.hadoop.mapreduce.lib.input.FileInputFormat;
import org.apache.hadoop.mapreduce.lib.output.FileOutputFormat;

public class Driver1 {

    public static void main(String[] args) throws Exception {

        Configuration conf = new Configuration();
        Job job = Job.getInstance(conf, "Word Count");

        job.setJarByClass(Driver1.class);

        job.setMapperClass(Mapp.class);
        job.setCombinerClass(Reducel.class);
        job.setReducerClass(Reducel.class);
        job.setPartitionerClass(Mypat.class);

        job.setNumReduceTasks(4);

        job.setOutputKeyClass(Text.class);
        job.setOutputValueClass(IntWritable.class);

        FileInputFormat.addInputPath(job, new Path(args[0]));
        FileOutputFormat.setOutputPath(job, new Path(args[1]));

        System.exit(job.waitForCompletion(true) ? 0 : 1);
    }
}

🟨 Mapper Class

import java.io.IOException;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.LongWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Mapper;

public class Mapp extends Mapper<LongWritable, Text, Text, IntWritable> {
    public void map(LongWritable key, Text value, Context context)
        throws IOException, InterruptedException {

    String[] words = value.toString().split(" ");

    for (String word : words) {
        context.write(new Text(word), new IntWritable(1));
    }
  }
}

🟨 Reducer Class

import java.io.IOException;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Reducer;

public class Reducel extends Reducer<Text, IntWritable, Text, IntWritable> {

    public void reduce(Text key, Iterable<IntWritable> values, Context context)
            throws IOException, InterruptedException {

        int sum = 0;
        for (IntWritable val : values) {
            sum += val.get();
        }
        context.write(key, new IntWritable(sum));
    }
}

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3)Write the procedure to load the data using internal tables in Hive Environment.
🔹 Step 1: Start Hive
hive

🔹 Step 2: Create Database (Optional)
CREATE DATABASE college;
USE college;

🔹 Step 3: Create Internal Table
CREATE TABLE student (
    id INT,
    name STRING,
    marks INT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

🎯 Example Input File (student.txt)
1,John,80
2,Alice,90
3,Bob,70


🔹 Step 4: Load Data into Table
LOAD DATA LOCAL INPATH '/student.txt'
INTO TABLE student;

🔹 Step 5: Verify Data
SELECT * FROM student;

🔹 Step 6: Check Table Structure
DESCRIBE student;

🔹 Step 7: Drop Table (Important)
DROP TABLE student;

🎯 Example Output
1   John   80
2   Alice  90
3   Bob    70



Set - 7

1.Implement Map Data Structure in Java

import java.util.LinkedHashMap;

public class MapExample {
    public static void main(String[] args) {

        LinkedHashMap<Integer, String> map = new LinkedHashMap<>();

        map.put(101, "Aaliyah");
        map.put(102, "Taylor");
        map.put(103, "Zayn");

        System.out.println("Map: " + map);

        System.out.println("Size: " + map.size());

        System.out.println("Student with ID 102: " + map.get(102));

        if (map.containsKey(103)) {
            System.out.println("Key 103 exists");
        }
    }
}


🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2.Write a Pig Latin scripts to demonstrate different filtering operators on your data.
🔹 Load Data
A = LOAD 'emp.txt' USING PigStorage(',') 
AS (id:int, name:chararray, dept:chararray, salary:int);

🟢 1. FILTER (Basic)
B = FILTER A BY salary > 30000;
DUMP B;

🟢 2. FILTER with AND
C = FILTER A BY dept == 'Manager' AND salary > 50000;
DUMP C;

🟢 3. FILTER with OR
D = FILTER A BY dept == 'Clerk' OR salary > 50000;
DUMP D;

🟢 4. FILTER with NOT
E = FILTER A BY NOT (dept == 'Clerk');
DUMP E;

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3.Implement the Pig Latin Script to find the max temp for each and every year

records = LOAD '/home/hadoop/sample.txt'
USING PigStorage(',')
AS (year:chararray, temperature:int, quality:int);

filtered_records = FILTER records BY temperature != 9999 AND
(quality == 0 OR quality == 1 OR quality == 4 OR quality == 5 OR quality == 9);

grouped_records = GROUP filtered_records BY year;

max_temp = FOREACH grouped_records GENERATE group,
MAX(filtered_records.temperature);

DUMP max_temp;


🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

4.Write the procedure to create and alter tables with example in Hive Environment.
🟨 Create Table
🔹Step 1: Start Hive
hive

🔹Step 2: Create Database (Optional)
CREATE DATABASE company;
USE company;

🔹Step 3: Create Table
CREATE TABLE emp (
    id INT,
    name STRING,
    salary FLOAT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

🔹 Step 4: Load Data into Table
LOAD DATA LOCAL INPATH '/emp.txt'
INTO TABLE emp;

🔹 Step 5: Display Data
SELECT * FROM emp;

🟨 Alter Table Operations
✔ 1. Add Column
ALTER TABLE emp ADD COLUMNS (age INT);

✔ 2. Change Column Name/Type
ALTER TABLE emp CHANGE salary salary DOUBLE;

✔ 5. Describe Table
DESCRIBE emp;

✔ 3. Rename Table
ALTER TABLE emp RENAME TO employee;

✔ 4. Drop Table
DROP TABLE employee;



Set - 8

1.Implement Set Data Structure in Java

import java.util.*;

public class SetExample {
    public static void main(String[] args) {

        // HashSet (no order)
        Set<String> set = new HashSet<>();
        set.add("Red");
        set.add("Green");
        set.add("Blue");

        System.out.println("Set: " + set);

        // TreeSet (sorted)
        Set<String> sortedSet = new TreeSet<>(set);
        System.out.println("Sorted Set: " + sortedSet);
    }
}


🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2.Implement the Pig Latin Script to find Word Count

lines = LOAD '/user/hadoop/HDFS_File.txt' AS (line:chararray);
words = FOREACH lines GENERATE FLATTEN(TOKENIZE(line)) AS word;
grouped = GROUP words BY word;
wordcount = FOREACH grouped GENERATE group, COUNT(words);
DUMP wordcount;


🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3.Demonstrate Order by and Group by clause in Hive Environment
🔹 Create Table
CREATE TABLE emp (
    id INT,
    name STRING,
    dept STRING,
    salary INT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

🔹 Load Data
LOAD DATA LOCAL INPATH '/home/hadoop/emp.txt'
INTO TABLE emp;

🟢 ORDER BY
SELECT * FROM emp ORDER BY salary;

SELECT * FROM emp ORDER BY salary DESC;

🟢 GROUP BY
SELECT dept, SUM(salary) 
FROM emp 
GROUP BY dept;


🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

4.Describe HDFS Commands for Adding , Retrieving and Deleting Files 

🟢 Adding Files
hdfs dfs -mkdir /input
hdfs dfs -put file.txt /input

🔵 Retrieving Files
hdfs dfs -cat /input/file.txt     # View file
hdfs dfs -get /input/file.txt     # Download to local

🔴 Deleting Files
hdfs dfs -rm /input/file.txt
hdfs dfs -rm -r /input/folder


Set - 9

1.Write a Pig Latin scripts to demonstrate distinct, Foreach operator on your data.
🔹 Step 1: Sample Data (student.txt)
1,John,80
2,Alice,90
3,Bob,80
4,John,70

🔹 Step 2: Load Data
A = LOAD 'student.txt' USING PigStorage(',') 
    AS (id:int, name:chararray, marks:int);

🔹 Step 3: DISTINCT (remove duplicates)
B = DISTINCT A;

🔹 Step 5: FOREACH 
D = FOREACH A GENERATE name, marks;

🔹 Step 6: Display Output
DUMP B;
DUMP D;

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2.Write a Map Reduce Program to implement Matrix Multiplication 

Input Format:
A 0 0 1
A 0 1 2
A 1 0 3
A 1 1 4
B 0 0 1
B 0 1 2
B 1 0 3
B 1 1 4

🟨Mapper Class:

import java.io.IOException;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Mapper;

public class Matmap extends Mapper<Object, Text, Text, Text> {

    public void map(Object key, Text value, Context context)
            throws IOException, InterruptedException {

        String[] data = value.toString().split(" ");
        String matrix = data[0];
        int row = Integer.parseInt(data[1]);
        int col = Integer.parseInt(data[2]);
        int val = Integer.parseInt(data[3]);

        if (matrix.equals("A")) {
            for (int j = 0; j < 2; j++) {
                context.write(
                        new Text(row + "," + j),
                        new Text("A," + col + "," + val)
                );
            }
        }
        else {
            for (int i = 0; i < 2; i++) {
                context.write(
                        new Text(i + "," + col),
                        new Text("B," + row + "," + val)
                );
            }
        }
    }
}


🟨Reducer Class:

import java.io.IOException;
import java.util.HashMap;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Reducer;

public class Matreduce extends Reducer<Text, Text, Text, Text> {
    public void reduce(Text key, Iterable<Text> values, Context context)
            throws IOException, InterruptedException {

        HashMap<Integer, Integer> mapA = new HashMap<>();
        HashMap<Integer, Integer> mapB = new HashMap<>();

        for (Text val : values) {
            String[] data = val.toString().split(",");
            String matrix = data[0];
            int index = Integer.parseInt(data[1]);
            int value = Integer.parseInt(data[2]);

            if (matrix.equals("A")) {
                mapA.put(index, value);
            } else {
                mapB.put(index, value);
            }
        }

        int sum = 0;
        for (int k : mapA.keySet()) {
            if (mapB.containsKey(k)) {
                sum += mapA.get(k) * mapB.get(k);
            }
        }

        context.write(key, new Text(String.valueOf(sum)));
    }
}


🟨Driver Class:

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Job;
import org.apache.hadoop.mapreduce.lib.input.FileInputFormat;
import org.apache.hadoop.mapreduce.lib.output.FileOutputFormat;

public class Matdriver {

    public static void main(String[] args) throws Exception {

        if (args.length != 2) {
            System.out.println("Usage: MatrixDriver <input path> <output path>");
            System.exit(-1);
        }
        Configuration conf = new Configuration();
        Job job = Job.getInstance(conf, "Matrix Multiplication");

        job.setJarByClass(Matdriver.class);
        job.setMapperClass(Matmap.class);
        job.setReducerClass(Matreduce.class);

        job.setOutputKeyClass(Text.class);
        job.setOutputValueClass(Text.class);

        FileInputFormat.addInputPath(job, new Path(args[0]));
        FileOutputFormat.setOutputPath(job, new Path(args[1]));

        System.exit(job.waitForCompletion(true) ? 0 : 1);
    }
}



Output Format:
0,0 7
0,1 10
1,0 15
1,1 22

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3) Write the procedure to create, drop and rename tables with example in Hive Environment

CREATE DATABASE company;
USE company;

CREATE TABLE emp (
    id INT,
    name STRING,
    salary FLOAT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

ALTER TABLE emp RENAME TO employee;

DROP TABLE employee;




Set -10

1.Write a Pig Latin scripts to demonstrate Union, Split operator on your data.
🔹 Sample Data
---file1.txt
1,John,80
2,Alice,90
---file2.txt
3,Bob,70
4,David,85

🔹 Step 1: Load Data
A = LOAD 'file1.txt' USING PigStorage(',') 
    AS (id:int, name:chararray, marks:int);

B = LOAD 'file2.txt' USING PigStorage(',') 
    AS (id:int, name:chararray, marks:int);

🔹UNION Operator
C = UNION A, B;
DUMP C;

✔ Output (UNION)
1,John,80
2,Alice,90
3,Bob,70
4,David,85

🔹SPLIT Operator
SPLIT C INTO 
    pass IF marks >= 80, 
    fail IF marks < 80;
🔹 Display Results
DUMP pass;
DUMP fail;

✔ Output (SPLIT)
Pass (marks ≥ 80)
1,John,80
2,Alice,90
4,David,85

Fail (marks < 80)
3,Bob,70

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

2)Write a Map Reduce Program to find Max temperature for a given NCDC weather dataset.
🟨 Mapper Class

import java.io.IOException;
import org.apache.hadoop.io.LongWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.mapreduce.Mapper;

public class MaxTempMap extends Mapper<LongWritable, Text, Text, IntWritable> {

    public void map(LongWritable key, Text value, Context context)
            throws IOException, InterruptedException {

        String line = value.toString();

        String year = line.substring(15, 19); 

        int airtemp;
        if (line.charAt(87) == '+') {
            airtemp = Integer.parseInt(line.substring(88, 92));
        } else {
            airtemp = Integer.parseInt(line.substring(87, 92));
        }

        String quality = line.substring(92, 93);

        if (airtemp != 9999 && quality.matches("[01459]")) {
            context.write(new Text(year), new IntWritable(airtemp));
        }
    }
}

🟨 Reducer Class

import java.io.IOException;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Reducer;

public class MaxTempReduce extends Reducer<Text, IntWritable, Text, IntWritable> {

    public void reduce(Text key, Iterable<IntWritable> values, Context context)
            throws IOException, InterruptedException {

        int maxvalue = Integer.MIN_VALUE;

        for (IntWritable value : values) {
            maxvalue = Math.max(maxvalue, value.get());
        }

        context.write(key, new IntWritable(maxvalue));
    }
}

🟨 Driver Class

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Job;
import org.apache.hadoop.mapreduce.lib.input.FileInputFormat;
import org.apache.hadoop.mapreduce.lib.output.FileOutputFormat;

public class MaxTempDriver {

    public static void main(String[] args) throws Exception {

        Configuration conf = new Configuration();
        Job job = Job.getInstance(conf, "Max Temperature");

        job.setJarByClass(MaxTempDriver.class);
        job.setMapperClass(MaxTempMap.class);
        job.setReducerClass(MaxTempReduce.class);

        job.setOutputKeyClass(Text.class);
        job.setOutputValueClass(IntWritable.class);

        FileInputFormat.addInputPath(job, new Path(args[0]));
        FileOutputFormat.setOutputPath(job, new Path(args[1]));

        System.exit(job.waitForCompletion(true) ? 0 : 1);
    }
}

🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿🧿

3)Write the procedure to load the data using internal tables in Hive Environment.
🔹 Step 1: Start Hive
hive

🔹 Step 2: Create Database (Optional)
CREATE DATABASE college;
USE college;

🔹 Step 3: Create Internal Table
CREATE TABLE student (
    id INT,
    name STRING,
    marks INT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

🎯 Example Input File (student.txt)
1,John,80
2,Alice,90
3,Bob,70


🔹 Step 4: Load Data into Table
LOAD DATA LOCAL INPATH '/student.txt'
INTO TABLE student;

🔹 Step 5: Verify Data
SELECT * FROM student;

🔹 Step 6: Check Table Structure
DESCRIBE student;

🔹 Step 7: Drop Table (Important)
DROP TABLE student;

🎯 Example Output
1   John   80
2   Alice  90
3   Bob    70


